/**
 * @fileOverview Firebase configuration file.
 *
 * This file is now mostly empty as all Firebase services have been replaced
 * with local mocks. The exported objects are kept for compatibility
 * with other files that may import from here, but they are empty.
 */

const app = {};
const db = {};
const storage = {};

export { app, db, storage };
